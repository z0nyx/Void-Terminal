# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects generated by `expo prebuild` (regenerated from app.json +
# modules/* on every prebuild — not hand-edited, so not worth versioning)
/android/
/ios/

# Native build outputs, if the native projects are ever built in place
*.apk
*.aab
*.ipa
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
android/app/build/
android/build/
android/.gradle/
android/local.properties
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/
*.xcuserstate
DerivedData/

# EAS / Metro / testing
.metro-health-check*
*.tsbuildinfo
coverage/

# Env / secrets — never commit real credentials
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.vscode/
.idea/

# Design-only reference material (the original Claude Design mockup this
# app was built from) — kept locally for reference, not part of the
# shipped app or its build.
design-reference/uploads/
design-reference/.thumbnail
